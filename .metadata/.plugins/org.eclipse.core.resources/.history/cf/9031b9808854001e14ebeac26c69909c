package com.Dao;

import java.sql.Connection;
import java.sql.PreparedStatement;


import com.cofig.MyCofigurationBank;
import com.model.Bankinfromation;

public class BankinfromationDao {
	public int insertBankinfromation(Bankinfromation b) {
		int check =0;
		PreparedStatement pst = null;
		 Connection con=MyCofigurationBank.getcConnection();
		 String query = "insert into Bankinfromation value (?,?,?,?,?)";
		 try {
			 MyCofigurationBank.preparedStatementSet(pst, con, query, b);
			}
		 finally {
			 MyCofigurationBank.closeconnection(pst, con);
		 }
		 
		return check;
		}
		
	}


